name: Makima-MD 
services:
  - name: MAKIMA-MD-SERVICE
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
